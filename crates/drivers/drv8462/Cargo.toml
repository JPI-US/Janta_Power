[package]
name = "drv8462"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
accel-stepper.workspace = true
esp-idf-svc = { workspace = true, features = [
    "critical-section",
    "embassy-time-driver",
    "embassy-sync",
] }
log.workspace = true